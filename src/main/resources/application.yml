
server:
  port: 8171

spring:
  application:
    name: SpringBoot_OPEN_AUTHENTICATION_2.0
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${client-id}
            client-secret: ${client-secret}
#            redirect-uri: http://localhost:8080/login/oauth2/code/github
#            scope: user:email
